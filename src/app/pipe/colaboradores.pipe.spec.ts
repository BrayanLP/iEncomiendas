import { ColaboradoresPipe } from './colaboradores.pipe';

describe('ColaboradoresPipe', () => {
  it('create an instance', () => {
    const pipe = new ColaboradoresPipe();
    expect(pipe).toBeTruthy();
  });
});
